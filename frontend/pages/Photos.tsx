import PageLayout from "../components/Page/PageLayout"

export default function Photos() {
  return <PageLayout pageTitle={"photos"}>
    <div className="blogPosts">
      <div className="empty">
        Cleaning the dust off the pile of photos collected over the years.
      </div>
    </div>
  </PageLayout>
}
